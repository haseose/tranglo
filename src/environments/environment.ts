// Development environment — API key is a placeholder.
// The real key is injected via EXCHANGE_RATE_API_KEY at build/run time.
export const environment = {
  production: false,
  exchangeRateApiKey: '',
  exchangeRateApiBaseUrl: 'https://v6.exchangerate-api.com/v6',
};
